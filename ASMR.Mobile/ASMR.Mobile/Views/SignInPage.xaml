<?xml version="1.0" encoding="utf-8" ?>
<v:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:v="clr-namespace:ASMR.Mobile.Views;assembly=ASMR.Mobile"
             xmlns:vm="clr-namespace:ASMR.Mobile.ViewModels;assembly=ASMR.Mobile"
             mc:Ignorable="d"
             x:Class="ASMR.Mobile.Views.SignInPage"
             x:DataType="vm:SignInViewModel"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             Shell.PresentationMode="ModalAnimated"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Spacing="16" Margin="16,32" VerticalOptions="CenterAndExpand">
            <Label Text="asmr"
                   FontFamily="Brand-Bold"
                   FontSize="48"
                   TextColor="{StaticResource Primary}"
                   Margin="0, 0, 0, 32"
                   HorizontalTextAlignment="Center"  />
            <Entry Text="{Binding Username}"
                   Placeholder="Username" />
            <Entry Text="{Binding Password}"
                   IsPassword="True"
                   Placeholder="Password"
                   ReturnCommand="{Binding SignInCommand}" />
            <Button Text="Sign In"
                    FontFamily="Content-Bold"
                    FontSize="16"
                    HorizontalOptions="FillAndExpand"
                    Command="{Binding SignInCommand}" />
            <Label Text="{Binding SignInStatus}"
                   HorizontalOptions="FillAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</v:BaseContentPage>